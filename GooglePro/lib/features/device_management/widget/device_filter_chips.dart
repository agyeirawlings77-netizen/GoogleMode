import 'package:flutter/material.dart'; import '../model/device_filter.dart'; import '../../../core/theme/app_theme.dart'; class DeviceFilterChips extends StatelessWidget { final DeviceFilter selected; final ValueChanged<DeviceFilter> onChanged; const DeviceFilterChips({super.key, required this.selected, required this.onChanged}); @override Widget build(BuildContext context) => Row(children: DeviceFilter.values.map((f) => GestureDetector(onTap: () => onChanged(f), child: Container(margin: const EdgeInsets.only(right: 8), padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6), decoration: BoxDecoration(color: selected == f ? AppTheme.primaryColor.withOpacity(0.1) : AppTheme.darkCard, borderRadius: BorderRadius.circular(20), border: Border.all(color: selected == f ? AppTheme.primaryColor.withOpacity(0.4) : AppTheme.darkBorder)), child: Text(f.label, style: TextStyle(color: selected == f ? AppTheme.primaryColor : AppTheme.darkSubtext, fontSize: 12, fontWeight: selected == f ? FontWeight.w600 : FontWeight.normal))))).toList()); }