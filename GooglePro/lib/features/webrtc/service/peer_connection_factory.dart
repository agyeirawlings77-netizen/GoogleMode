import 'package:flutter_webrtc/flutter_webrtc.dart'; import 'package:injectable/injectable.dart'; import '../model/webrtc_config.dart'; import '../../../core/utils/app_logger.dart'; @injectable class PeerConnectionFactory { Future<RTCPeerConnection> create() async { final pc = await createPeerConnection(WebRtcConfig.iceServers); AppLogger.info('PeerConnection created'); return pc; } void configureEncodings(RTCPeerConnection pc, {int? maxBitrate, int? maxFramerate}) { AppLogger.debug('Encoding: ${maxBitrate}bps @ ${maxFramerate}fps'); } }