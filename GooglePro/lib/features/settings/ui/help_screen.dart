import 'package:flutter/material.dart'; import 'package:go_router/go_router.dart'; import '../../../core/theme/app_theme.dart'; class HelpScreen extends StatelessWidget { const HelpScreen({super.key}); @override Widget build(BuildContext context) { return Scaffold(backgroundColor: AppTheme.darkBg, appBar: AppBar(title: const Text('Help & Support', style: TextStyle(color: AppTheme.darkText, fontWeight: FontWeight.w700)), backgroundColor: AppTheme.darkSurface, elevation: 0, leading: IconButton(icon: const Icon(Icons.arrow_back, color: AppTheme.darkText), onPressed: () => context.pop())), body: ListView(padding: const EdgeInsets.all(24), children: [_faq('How do I add a device?', 'Tap the + button on the dashboard and scan the QR code shown on the other device.'), _faq('How does auto-connect work?', 'After pairing, trusted devices reconnect automatically when they come online, even after reboot.'), _faq('Is my screen share encrypted?', 'Yes. All WebRTC connections are end-to-end encrypted using DTLS.'), _faq('What is the screen share range?', 'GooglePro uses P2P WebRTC — no distance limit as long as both devices have internet.')])); } Widget _faq(String q, String a) => Padding(padding: const EdgeInsets.only(bottom: 20), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text(q, style: const TextStyle(color: AppTheme.darkText, fontWeight: FontWeight.w700, fontSize: 15)), const SizedBox(height: 6), Text(a, style: const TextStyle(color: AppTheme.darkSubtext, height: 1.6))])); } }