import 'package:dartz/dartz.dart'; import 'package:injectable/injectable.dart'; import '../../entities/device_entity.dart'; import '../../repositories/i_device_repository.dart'; @injectable class RegisterDeviceUseCase { final IDeviceRepository _repo; RegisterDeviceUseCase(this._repo); Future<Either<String, DeviceEntity>> call(DeviceEntity device) => _repo.registerDevice(device); }
