import 'package:dartz/dartz.dart'; import 'package:injectable/injectable.dart'; import '../../entities/session_entity.dart'; import '../../repositories/i_session_repository.dart'; @injectable class StartSessionUseCase { final ISessionRepository _repo; StartSessionUseCase(this._repo); Future<Either<String, SessionEntity>> call({required String hostId, required String viewerId, required SessionType type}) => _repo.startSession(hostId: hostId, viewerId: viewerId, type: type); }
